-- Откат 0001.
-- Down-миграция нужна не для прода (там откат схемы обычно опаснее
-- наката исправления), а для CI: накатить → откатить → накатить снова
-- — это единственная дешёвая проверка воспроизводимости миграции.
DROP TABLE IF EXISTS answers.answers;
DROP SCHEMA IF EXISTS answers;
